#include <iostream>

int main() {
    int k;
    std::cout << "Enter the value of 'k': ";
    std::cin >> k;

    for (int i = 2; i <= 10; ++i) {
        std::cout << k << " x " << i << " = " << k * i << std::endl;
    }

    return 0;
}
