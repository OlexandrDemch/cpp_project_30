#include <iostream>

int main() {
    int sum = 0;
    for (int i = 1; i <= 1000; ++i) {
        sum += i;
    }

    double average = static_cast<double>(sum) / 1000;

    std::cout << "Average of integers from 1 to 1000 is: " << average << std::endl;

    return 0;
}
